# Usar plantillas por entorno:
#   cp .env.local.example .env.local       → desarrollo local
#   cp .env.dev.example .env.dev           → servidor dev/staging
#   cp .env.production.example .env        → VPS / producción (Docker)
